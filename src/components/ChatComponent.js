import LicenseService from '../services/LicenseService';

const ChatComponent = ({ apiKey, theme = 'light', userId }) => {
  return {
    // Этот компонент будет настроен в проекте пользователя
    // Здесь просто заглушка
    init() {
      console.log('Nexion Chat Component initialized');
    }
  };
};

export { ChatComponent };